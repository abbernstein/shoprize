1. Install nodejs
2. Install Grunt. In the terminal/command prompt, run "npm install -g grunt-cli"
3. Install the dependencies. Run "npm install" dont do bower installl (libraries were overriden)!!!!1 // & bower install"
4. Start the dev server. Run "grunt serve"



for deployment make sure to copy manually the iamges folder
